#if defined _left_4_ai_included
  #endinput
#endif
#define _left_4_ai_included

// Provides access to the left_4_ai plugin's convars and command names.
// No natives are exposed; include this file so other scripts can detect
// the plugin via LibraryExists("left_4_ai").
